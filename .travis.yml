language: node_js
node_js:
  - '8'
  - '6'
script:
  - npm run test
  - npm run build
branches:
  only:
    - master
install:
  - npm install -g codecov
script:
  - istanbul cover ./node_modules/mocha/bin/_mocha --reporter lcovonly -- -R spec
  - codecov
